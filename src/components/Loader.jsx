import React from "react";
import { StyledLoader } from "../styles/styled-components";

const Loader = () => {
  return (
    <StyledLoader>
      <span className="loader"></span>
    </StyledLoader>
  );
};

export default Loader;
